This dll should not be referenced by the application in which it is included. 
Since IoC and DI is being used, the application should not know anything about this dll except by the common interface.
The dll will need to be manually copied to any application that uses it.
